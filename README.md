Make a memory matching game use only js and dom. 1 step is is to create a card using for loop and second step is to check card if they not match then flip the card, 
if match don't flip the card (remove the classlist called flipped). 
